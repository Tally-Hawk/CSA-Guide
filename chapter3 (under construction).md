# **مبانی لینوکس** 🐧

### گوش کنید به نسخه کوتاه به زبان فارسی 🎧 [Audio](https://open.spotify.com/episode/3C1uuXch9hml7QcCXurwy1?si=4hSIuWLeTQKEISonoE6a3w)

**لینوکس**، این شاهکار دنیای نرم‌افزارهای آزاد، یک سیستم‌عامل قدرتمند، پایدار و انعطاف‌پذیر است که ریشه در دل فلسفهٔ متن‌باز (Open-Source) دارد. این سیستم‌عامل که امروز در سرورها، ابررایانه‌ها، گوشی‌های هوشمند و حتی ماهواره‌ها نفس می‌کشد، از دل عشق به آزادی نرم‌افزاری و همکاری جهانی زاده شده است. بیش از **۶۰۰ توزیع (Distribution)** مختلف از لینوکس وجود دارد که هر یک برای هدفی خاص طراحی شده‌اند؛ از اوبونتوِ دوست‌داشتنی گرفته تا رد هتِ سازمانی.

داستان لینوکس از دههٔ ۱۹۷۰ و سیستم‌عامل **یونیکس** (متعلق به شرکت AT&T) آغاز می‌شود. در سال ۱۹۹۱، جوانی فنلاندی به نام **لینوس توروالدز** تصمیم گرفت هسته‌ای آزاد و مستقل بسازد که همگان بتوانند آن را ببینند، تغییر دهند و بهبود ببخشند. بدین ترتیب **اولین نسخهٔ لینوکس** متولد شد. لینوکس به دلیل متن‌باز بودن، بسیار کمتر از ویندوز در معرض بدافزارها قرار دارد و جامعهٔ عظیمی از توسعه‌دهندگان در سراسر جهان، شبانه‌روز آن را محافظت و ارتقا می‌دهند.

فلسفهٔ لینوکس همچون شعری از سادگی و زیبایی است:  
- **همه چیز یک فایل است**، حتی سخت‌افزارها!  
- برنامه‌ها کوچک، تک‌منظوره و حرفه‌ای هستند.  
- با اتصال این ابزارهای کوچک به هم (مثل لوله‌کشی هوشمند)، می‌توان کارهای بسیار پیچیده انجام داد.  
- از رابط‌های کاربری بسته و اسارت‌آور دوری می‌شود.  
- تمام تنظیمات در فایل‌های متنی ساده ذخیره می‌گردند تا انسان بر ماشین مسلط باشد، نه برعکس.

**معماری لینوکس** مانند ساختمانی هوشمند و منظم است که از پایین به بالا شامل این لایه‌هاست:  
- **سخت‌افزار** ← **هسته (Kernel)** ← **شل (Shell)** ← **برنامه‌های سیستمی و کاربردی**  
اجزایی چون **بوت‌لودر** (گراب)، **دیمن‌ها** (خدمات پس‌زمینه)، **سرور گرافیکی** (مانند Xorg یا Wayland)، **مدیر پنجره** (مثل GNOME و KDE) و صدها ابزار کاربردی، این اکوسیستم را کامل می‌کنند.

سلسله‌مراتب فایل‌ها در لینوکس همچون نقشهٔ یک شهر باستانی منظم و دقیق است:  
- **/** → ریشهٔ همه چیز  
- **/home** → خانهٔ کاربران  
- **/etc** → قلب تنظیمات سیستم  
- **/var** → جایی برای لاگ‌ها و فایل‌های متغیر  
- **/usr** → خانهٔ برنامه‌ها و کتابخانه‌ها  
- **/tmp** → محل فایل‌های موقت  
و دیگر دایرکتوری‌هایی که هر یک وظیفه‌ای مقدس دارند.

در پایان، باید گفت که دروازهٔ واقعی ورود به دنیای لینوکس، **ترمینال** و **شل** است. معروف‌ترین شل، **BASH** (Bourne Again SHell) نام دارد. در این محیط متنیِ ساده اما بی‌نهایت قدرتمند، با چند دستور کوتاه می‌توان سرور مدیریت کرد، اسکریپت نوشت، سیستم را تحلیل کرد و حتی عاشق لینوکس شد!  
لینوکس نه فقط یک سیستم‌عامل، بلکه یک **سبک زندگی**، یک **فلسفهٔ آزادی** و یک **هنر دیجیتال** است.

### شِل (Shell) لینوکس 🐧

### گوش کنید به نسخه کوتاه به زبان فارسی 🎧 [Audio](https://open.spotify.com/episode/6q06t5S5zDoBE4UyJkaZCh?si=FmiTZxHmTCiak35liyPOoA)

**پرامپت (Prompt) چیست؟**
وقتی وارد ترمینال لینوکس می‌شوید، چیزی شبیه به این می‌بینید:  
`<username>@<hostname>:<current working directory>$` 

<img src="../main/files/shell.png" width="600">

- علامت `~` یعنی شما در پوشه خانگی (Home directory) خود هستید  
- علامت `$` نشان‌دهنده کاربر عادی (غیرمدیر) است  
- علامت `#` یعنی شما به‌عنوان root (مدیرکل سیستم با تمام اختیارات) وارد شده‌اید  

**چگونه کمک بگیریم؟** 📚  
لینوکس پر از مستندات داخلی است و نیازی به جست‌وجوی اینترنتی ندارید!  
- `man ls` → صفحه راهنمای کامل دستور ls  
- `ls --help` یا `ls -h` → خلاصه‌ای کوتاه و مفید از گزینه‌ها  
- `apropos ls` → پیدا کردن تمام دستوراتی که به «ls» مربوط هستند  
توصیه طلایی: هر وقت دستور جدیدی دیدید، اول `man` آن را بخوانید!

**دستورات مهم برای شناخت سیستم** 🔍  
با این دستورات می‌توانید در چند ثانیه سیستم خود را کامل بشناسید:  
- `whoami` → من کی‌ام؟  
- `id` → شناسه کاربری و گروهی  
- `hostname` → نام کامپیوتر  
- `uname -a` → تمام اطلاعات کرنل  
- `pwd` → در کدام پوشه هستم؟  
- `ip a` یا `ifconfig` → وضعیت کارت شبکه  
- `ss` یا `netstat` → اتصالات شبکه  
- `ps aux` → فرآیندهای در حال اجرا  
- `lsblk` → دیسک‌ها و پارتیشن‌ها  
- `lsusb` و `lspci` → دستگاه‌های متصل  
- `env` → متغیرهای محیطی  

**ورود از راه دور با SSH** 🔐  
ساده‌ترین و امن‌ترین راه اتصال به سرورهای لینوکس:  
`ssh user@host`  
مثال: `ssh ali@192.168.1.100` یا `ssh root@myserver.com`  

فقط همین چند خط را به خاطر بسپارید و یک گام بزرگ به‌سوی تسلط بر لینوکس برداشته‌اید ✨

### راهنمای دستورات پایه لینوکس 📘

### گوش کنید به نسخه کوتاه به زبان فارسی 🎧 [Audio](https://open.spotify.com/episode/5hqCUMWMilqKghHeXrrIiH?si=sy95xXdxRVWy4-8W65ZIgQ)

**ناوبری و جهت‌یابی در سیستم فایل** 🧭  
ترمینال لینوکس مانند یک نقشه‌ی زنده است. با `pwd` همیشه می‌دانید دقیقاً در کدام مسیر هستید (`/home/arash`). دستور `ls` محتویات پوشه را نشان می‌دهد و `ls -l` (یا `ll`) جزئیات کامل شامل مجوزها، مالک، اندازه و تاریخ را نمایش می‌دهد. برای رفتن به پوشه دیگر `cd <مسیر>` و برای بازگشت به خانه `cd ~` یا `cd` بزنید. اگر صفحه پر شد، یک `clear` یا `Ctrl+L` همه چیز را تمیز می‌کند.

**ساخت، کپی، جابه‌جایی و حذف فایل‌ها و پوشه‌ها** ✂️  
- `touch file.txt` → فایل خالی می‌سازد یا زمان آخرین تغییر فایل موجود را به‌روزرسانی می‌کند  
- `mkdir <نام>` → پوشه جدید می‌سازد؛ با `mkdir -p parent/child/grandchild` حتی پوشه‌های تودرتو را یکجا می‌سازد  
- `cp <منبع> <مقصد>` → کپی می‌کند (با `-r` برای پوشه‌ها)  
- `mv <منبع> <مقصد>` → هم برای تغییر نام و هم برای جابه‌جایی استفاده می‌شود  
- `rm <فایل>` و `rm -r <پوشه>` → حذف دائمی (با `-f` بدون تأی سؤال می‌پرسد، مراقب باشید!)  
- `tree .` → ساختار درختی پوشه فعلی را به زیبایی نمایش می‌دهد

**ویرایش و مشاهده سریع فایل‌ها** 📝  
`nano` ساده‌ترین ویرایشگر برای مبتدیان است (Ctrl+O ذخیره، Ctrl+X خروج).  
`vim` یا `vi` قدرتمندترین ویرایشگر است (حالت دستور با Esc، ذخیره و خروج با `:wq`).  
دستورات مشاهده سریع:  
- `cat file.txt` → تمام محتوا را یکجا نشان می‌دهد  
- `less file.txt` → صفحه به صفحه (با q خارج می‌شوید)  
- `head -n 10 file.txt` → ۱۰ خط اول  
- `tail -n 10 file.txt` → ۱۰ خط آخر (با `-f` برای مانیتورینگ لاگ عالی است)

**جستجوی فایل‌ها و برنامه‌ها 🔍  
- `which python3` → مسیر دقیق برنامه اجرایی را نشان می‌دهد  
- `whereis python3` → فایل باینری، صفحه راهنما و سورس را نمایش می‌دهد  
- `find /path -name "*.log"` → جستجوی دقیق و کند اما قدرتمند  
- `locate *.conf` → بسیار سریع (ابتدا باید `sudo updatedb` اجرا شود)

**پایپ، ریدایرکت و فیلترهای جادویی لینوکس** ✨  
لینوکس با سه جریان اصلی کار می‌کند:  
- **STDIN (0)** ← ورودی  
- **STDOUT (1)** → خروجی عادی  
- **STDERR (2)** → خطاها  

مثال‌های کاربردی:  
`ls /etc 2>/dev/null` → فقط خروجی درست را نشان بده، خطاها را دور بریز  
`cat file.txt | grep "error" | wc -l` → تعداد خطوط حاوی کلمه error  
`dmesg | tail -n 20` → آخرین ۲۰ خط پیام‌های کرنل

**ابزارهای پردازش متن (سوئیس آرمِی نایف لینوکس)** 🛠️  
`grep`, `sed`, `awk`, `cut`, `tr`, `sort`, `uniq`, `wc` — با ترکیب این ابزارها و پایپ می‌توانید تقریباً هر کاری روی متن انجام دهید. مثال زیبا:  
`ps aux | grep python | awk '{print $2}' | xargs kill -9` → تمام پروسه‌های پایتون را بکش!

**مجوزها و امنیت فایل‌ها** 🔐  
هر فایل ۱۰ کاراکتر مجوز دارد، مثلاً `-rwxr-xr--`:  
- موقعیت ۱: نوع (`-` فایل، `d` پوشه، `l` لینک)  
- موقعیت ۲-۴: صاحب (rwx)  
- موقعیت ۵-۷: گروه  
- موقعیت ۸-۱۰: دیگران  

- `chmod 755 script.sh` → صاحب همه مجوزها، بقیه فقط خواندن و اجرا  
- `chmod u+x file` → فقط به صاحب اجازه اجرا بده  
- `chown arash:developers file.txt` → تغییر مالک و گروه  
- **SUID** (4)، **SGID** (2) و **Sticky bit** (1) در عدد اکتال برای رفتارهای خاص (مثل `/usr/bin/passwd`)

### مدیریت سیستم در لینوکس 📖

### گوش کنید به نسخه کوتاه به زبان فارسی 🎧 [Audio]()

**مدیریت کاربران** یکی از مهم‌ترین وظایف مدیر سیستم است. با استفاده از دستورات قدرتمندی می‌توان کاربران جدید ساخت، دسترسی‌ها را تغییر داد یا کاربران را حذف کرد.  
- ایجاد یا تغییر کاربران و اجرای وظایف خاص  
- دستورات کلیدی:  
  • **sudo** – اجرای دستور با دسترسی روت  
  • **su** – تغییر به کاربر دیگر  
  • **useradd**, **usermod**, **userdel**  
  • **addgroup**, **delgroup**  
  • **passwd** – تغییر رمز عبور  

**مدیریت بسته‌ها (Package Management)** در اوبونتو و دبیان عمدتاً از طریق ابزارهای پیشرفته‌ای انجام می‌شود که دانلود، حل وابستگی‌ها، نصب و کنترل کیفیت را به‌صورت خودکار بر عهده دارند.  
- ابزارهای اصلی: **apt**, **aptitude**, **dpkg**  
- ابزارهای تکمیلی: **snap**, **pip**, **gem**, **git**  

**مدیریت سرویس‌ها و پروسه‌ها** بخش حیاتی هر سرور لینوکسی است. می‌توانید سرویس‌های سیستمی و کاربری را کنترل کنید، پروسه‌ها را مشاهده و در صورت نیاز متوقف نمایید.  
- مفاهیم مهم: PID و PPID  
- مدیریت سرویس: **systemctl**  
- دستورات خاتمه پروسه: **kill**, **pkill**, **pgrep**, **killall**  
- انتقال پروسه به پس‌زمینه با **bg** و بازگرداندن به پیش‌زمینه با **fg**

**زمان‌بندی وظایف (Task Scheduling)** امکان خودکارسازی کارهای تکراری را فراهم می‌کند. دو روش اصلی وجود دارد:  
- **Systemd Timer**: مدرن، دقیق و یکپارچه با systemd  
  • ساخت سرویس و تایمر + فعال‌سازی  
- **Cron**: روش کلاسیک و بسیار محبوب  
  • ویرایش crontab با دستور **crontab -e**

**خدمات شبکه** از مهم‌ترین بخش‌های یک سرور هستند.  
- **SSH**: دسترسی امن از راه دور  
  `sudo apt install openssh-server -y` → `systemctl status ssh` → `ssh user@host`  
- **NFS**: اشتراک‌گذاری فایل در شبکه محلی  
- **Apache2**: محبوب‌ترین وب‌سرور  
  `sudo apt install apache2 -y`  
- **سرور ساده پایتون**:  
  `sudo apt install python3 -y` → `python3 -m http.server 8000`

  <img src="../main/files/apache.png" width="600">

**VPN و امنیت ارتباطی** با OpenVPN به‌راحتی قابل راه‌اندازی است:  
`sudo apt install openvpn -y`  
`sudo openvpn --config client.ovpn`

در نهایت، ابزار **curl** امکان انتقال داده و تست وب‌سرویس‌ها را مستقیماً از ترمینال می‌دهد و جایگزین‌های آن wget یا اسکریپت‌های پایتون هستند. پس از نصب Apache یا سرور پایتون، با باز کردن آی‌پی سرور در مرورگر، صفحه پیش‌فرض خوش‌آمدگویی نمایش داده می‌شود که نشان‌دهنده موفقیت‌آمیز بودن راه‌اندازی است. 💡
